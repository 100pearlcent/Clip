import styled from "styled-components";

let DashboardTitle = styled.span`
    font-family: "LeagueSpartan-Bold";
    font-size: 4.5vh;
    color: ${props=> props.theme.dimGray};
`

export default DashboardTitle;