import styled from 'styled-components';

let Quote = styled.div`
    font-family: "BlendaScript";
    color: ${props=> props.theme.blue};
    font-size: 9vh;
    text-align: center;
`;

export default Quote;
